// Goal: Close out the api-client unification round (cf2d1cb) — GOAL-09011
// 1. Migrate the LAST raw JSON fetch in src/pages/admin/Products.tsx:296 (save-product PUT/POST) to jsonFetch.
// 2. Live verification, dual-engine (WebKit + Chromium), migrated pages only:
//    /, /products, /brands, /offers, /blog, /contact, /compare?  → 0 console errors, 0 failed requests.
// 3. Real header-search flow on /products (type query → results render → Enter navigates).
// 4. Commit + push; PROJECT_GRAPH.md updated; report terse Persian with evidence.
// Rule: no new scope beyond these 4 items; boy-scout exemptions stay untouched.
