// GOAL-09011b: follow-up round after 2026-09-11 unification.
// 1. LIVE: SAMAN_TERMINAL_ID=636806 appended to VPS .env (from SECRETS_MAP) +
//    docker compose up -d --force-recreate. Verify env inside container, health,
//    saman/failover markers in served server.cjs, live 200s.
// 2. Boy-scout admin/Orders.tsx: kill the 4 `any`s (handleCopySMS, handlePrintInvoice,
//    items maps) with a real AdminOrder type mirroring the DB row shape
//    (schema.ts orders + order_items: recipientName/recipientPhone/refId/qty flat row).
// 3. Gate (tsc+vitest+build), commit, push, deploy, byte-parity check.
// Scope lock: no other files; Home/Header untouched (hot churn); no SEO moves (GSC freeze).
