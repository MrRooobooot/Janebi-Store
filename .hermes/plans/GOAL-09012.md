// GOAL-09012: sitewide design-system polish — root-cause pass, not another spot edit.
// Diagnosis (screenshot + census): page-level components each hand-roll their own
// bg/border/text from 3 different neutral families (slate/gray/zinc) + hardcoded
// hexes, so edits never converge. Root cause = token bypass, not token choice.
// Items:
// 1. Token unification: add missing @theme tokens (--color-canvas, --color-surface,
//    --color-border, --color-text-muted, --color-text-main aliases) so components
//    have ONE canonical name per role; map legacy zinc/gray/slate drift to them in
//    the 4 layout files (index.css utilities section), not per-component.
// 2. ProductCard: fix card-CTA bleed (fixed footer with mt-auto), unify neutral
//    classes to tokens, placeholder tile = warm canvas token not cool slate-50.
// 3. Sidebar: count pills contrast (slate-600 on slate-100 → text-main/muted tokens),
//    hover rows get token bg, chat-fab clearance (pb-28 on lg sidebar bottom).
// 4. Products page header: hero band gradient → soft two-tone rose-tinted canvas
//    (Iranian taste: warm, not pastel-wash), subtext contrast ≥4.5:1.
// 5. Verify: tsc, vitest, build, dual-engine sweep on /, /products (390px + 1280px).
// Scope lock: no logic changes, no route changes, no admin pages this round.
