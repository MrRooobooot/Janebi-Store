// GOAL-09013: dark/light contrast — root-cause pass.
// Diagnosis (4 screenshots, all dark mode): the CSS-layer neutral shims added in
// GOAL-09012 only map LIGHT-mode neutrals; in .dark the raw slate/gray classes
// still resolve to Tailwind's dark palettes where components hand-rolled them,
// AND several tiles/cards are "stuck light" (bg-white/* without dark: sibling,
// bg-white/90 tiles, category icon wells, user pill, hero slide text/badge,
// wholesale banner, value-prop cards, namad block, topbar text).
// Root cause = same token-bypass class, but the DARK half of the mapping was
// missing. Items:
// 1. index.css: add .dark shims mapping drift neutrals to dark role tokens
//    (bg-slate-50/100, bg-gray-50..800, text/border families), and map
//    .dark .bg-white → surface-dark family. Light shims unchanged.
// 2. Fix "stuck light" survivors individually: Home hero visual tile bg-white/90,
//    HeaderSearch gray-input (gray-800/80 = wrong family), user pill is fine
//    (has dark:) — verify only; edge-fade gradients from-white (Home carousel).
// 3. Footer zinc-700 light-only + Header slate-600 light-only: map or pair.
// 4. Re-run design-audit + extend it with a DARK-mode pass on / and /products
//    (same assertions under .dark) — 8 combos total.
// 5. Gate: tsc + vitest + build; deploy; byte-parity.
// Scope lock: CSS-first, minimal per-component edits, no admin, no logic.
