# Frontend Cart/Checkout UX Cluster — 2026-09-02

## Commit
- **Hash**: `78226bd` (main, pushed to origin)
- **Files**: `src/pages/Cart.tsx`, `src/pages/Checkout.tsx`, `src/components/cart/CartSummaryCard.tsx`, `src/components/cart/CartItemList.tsx`, `src/components/checkout/CheckoutStepsBar.tsx`, `src/components/checkout/CheckoutOrderSummary.tsx`, `src/components/EmptyState.tsx`, `src/hooks/useCartSummary.ts`, `src/index.css`

## Gate Result
- `npm run verify` → **ALL GREEN**: strict `tsc`, 322/322 Vitest tests (42 files), Vite client build + esbuild server bundle, artifact audit passed (`✅ ALL HARDCORE QUALITY GATES PASSED`).

## Changes
1. **Kinetic palette tokens**: order-summary cards + coupon feedback moved off hardcoded `orange-*` to `@theme` primary tokens — CTA `bg-primary-300/400` (#F47C20), price text `text-primary-400 dark:text-primary-300` (#EA580C); dual-theme light/dark crisp variants; no hardcoded surfaces.
2. **Coupon feedback**: inline success (`role=status`) + error (`role=alert`, dismissible, `aria-invalid`/`aria-describedby`) states in CartSummaryCard, driven by new `couponError`/`dismissCouponError` in `useCartSummary` — which also switched the coupon call to `authFetch` (project invariant).
3. **Checkout wizard steps**: `CheckoutStepsBar` rewritten data-driven with `currentStep` prop; distinct complete (check icon, linkable) / current (gradient + ring, `aria-current="step"`) / upcoming (muted) states; Persian digits via `toPersianDigits`; Cart page now reuses it at step 1 (removed duplicate inline stepper).
4. **A11y**: all icon-only cart buttons (±, trash, clear) get `min-touch-target` (≥44px) and Persian `aria-label`s; `prefers-reduced-motion` respected via `useReducedMotion` in Cart/Checkout/EmptyState/CartItemList plus a global CSS guard in `index.css`.

## Live Verification
- `GET https://janebiarena.ir/api/health` → **200** (`{"status":"ok","database":"ok",...}` per deploy health check)
- `GET /cart` → **200**, `GET /checkout` → **200**
- New bundle hashes live: `index-BiBTCCwB.js`, `Cart-DNTo6Mum.js`, `Checkout-CB99XFT0.js`, `CheckoutStepsBar-CGjsb4X2.js`, `EmptyState-BZp3wUkl.js` — these chunks were regenerated by this cluster's build (post-change) and match `dist/` after `./deploy.sh` (exit 0, "✅ Deploy OK").
