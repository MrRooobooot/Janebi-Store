# QA Report — 2026-09-03 Cluster (live https://janebiarena.ir)

Cluster 99983f3 (image perf: lazy loading + width/height on 21 img tags, ProductCard srcset/sizes, contrast r7 aeb8a6b). QA run: 2026-09-01 (+0330).

## GATE: `npm run verify`

**PASS** — `✅ ALL HARDCORE QUALITY GATES PASSED (100% VERIFIED)`; build emitted `dist/assets/index-BBveZfIU.js 140.28 kB │ gzip: 33.00 kB`, server bundle `dist/server.cjs 229.0kb`. Exit code 0.

## Live probes

| Check | Result | Evidence |
|---|---|---|
| /api/health | **PASS** | `{"status":"ok","database":"ok","latencyMs":1,"uptimeSeconds":145,"nodeVersion":"v22.23.2"}` |
| Home bundle hash | **PASS** | `curl -s https://janebiarena.ir/` → `index-BBveZfIU.js`; sha256 of remote `/assets/index-BBveZfIU.js` == local `dist/assets/index-BBveZfIU.js` = `df652921318be59261a56b30ce6a63a800f10fb31a8ececa4a558b1bcdd69170` |
| GET / | **PASS** | HTTP 200 |
| GET /products | **PASS** | HTTP 200 after canonical 301 → `/products/` (`location: /products/`); followed with -L → 200 |
| GET /product/14 | **PASS** | HTTP 200 (id 14 from `/api/products`) |
| /api/products test image | **PASS** | `grep -c '/images/test.jpg'` over full `/api/products` payload = **0** (no items) |
| loading=lazy on non-LCP imgs | **PASS** | Shared PictureImage in live bundle: `loading:a?`eager`:`lazy`` and `fetchPriority:a?`high`:`auto`` (LCP image eager+high, all others lazy). Standalone pages emit `loading:`lazy`` + `decoding:`async`` (ProductDetail, Products, Blog, Brands, Cart, Checkout, Compare, Orders, Profile chunks). ProductCard srcSet/sizes present: `srcSet:b/c/x/y`, `sizes:`(max-width: 640px) 45vw, (max-width: 1024px) 30vw, 240px`` |

Note: pages are client-rendered (SPA), so `loading="lazy"` appears in the JS bundles rather than raw HTML — verified against the sha256-identical live + local bundles.

## Verdict

**PASS — 8/8 checks green.** No failures.
