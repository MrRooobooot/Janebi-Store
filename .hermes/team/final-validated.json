[
 {
  "id": "R1-01",
  "severity": "major",
  "area": "payment",
  "file": "server/routes/payment.ts",
  "line": "22-56",
  "title": "POST /api/payment/request has no order-status guard — a payment link can be minted for an already-paid/cancelled order",
  "evidence": "const order = orderList[0];\n    // Verify order ownership\n    if (order.userId !== userId) {\n      return res.status(403).json({ error: 'Unauthorized to pay for this order' });\n    }\n    ...\n    if (paymentRequest.success && paymentRequest.authority) {\n      await db.update(orders)\n        .set({ authority: paymentRequest.authority })\n        .where(eq(orders.id, orderId));",
  "suggested_fix": "Only allow payment initiation when order.status === 'pending_payment' (return 400 otherwise) before calling paymentRouter.requestPaymentWithFailover and before overwriting `authority`.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "payment.ts:22-56 verified: after ownership check (line 30) there is NO status guard — /request proceeds for any order status including 'processing' (already paid), 'cancelled', 'shipped', 'delivered', and overwrites `authority` (lines 48-50) on the gateway response. A paid order can be re-minted a payment link; a cancelled order's authority can be re-pointed. Note the /verify side is idempotent (s"
 },
 {
  "id": "R2-01",
  "severity": "major",
  "area": "stale-closure",
  "file": "src/contexts/AuthContext.tsx",
  "line": "260, 285-288, 308-311, 331-337",
  "title": "Address CRUD functions close over stale `user` state — concurrent/repeated address mutations lose data",
  "evidence": "const addAddress = async (address) => { if (!user) return; ... setUser({ ...user, addresses: [...(user.addresses || []), data.address] }); } — same pattern in updateAddress (setUser({...user, addresses: user.addresses.map(...)})), deleteAddress and setDefaultAddress. All four spread the `user` captured at render time instead of using functional updates.",
  "suggested_fix": "Use functional updates: setUser(prev => prev ? { ...prev, addresses: [...] } : null) in all four address functions, so consecutive awaits (e.g. addAddress then setDefaultAddress) don't overwrite each other's result.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "All four address fns close over render-time `user`: AuthContext.tsx:260 `setUser({ ...user, addresses: [...(user.addresses || []), data.address] })`, :285-288, :308-311, :331-337 — none use functional setState. Contrast: updateProfile (line 235) and clearMustChangePassword (line 211) DO use `setUser(prev => ...)` correctly. Since each fn awaits an HTTP call before setUser, a second mutation issued"
 },
 {
  "id": "R2-02",
  "severity": "major",
  "area": "race-condition",
  "file": "src/contexts/CartContext.tsx",
  "line": "90-108",
  "title": "Guest-cart merge can silently drop guest items if a push fails, and is not aborted on logout",
  "evidence": "const pushGuest = guestItems.map(item => authFetch('/api/cart', {...}).catch(err => console.error('Failed to merge guest cart item', err))); Promise.all(pushGuest).then(() => authFetch('/api/cart', ...)).then(data => { if (Array.isArray(data)) { setCart(data); localStorage.setItem('cart', JSON.stringify(data)); } }) — a failed POST is swallowed, then the authoritative server list unconditionally REPLACES local state, destroying the guest item.",
  "suggested_fix": "Fail the merge (keep local state) when any push rejects, e.g. throw inside the catch so Promise.all rejects and the replace is skipped; or re-push failed items.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "CartContext.tsx:91-96: each pushGuest promise has `.catch(err => console.error(...))` returning undefined, so Promise.all NEVER rejects. Lines 99-108 then unconditionally `setCart(data)` + overwrite localStorage with the server list. A failed POST is silent data loss for that guest item. Also mergedForToken guard (line 87-88) means merge never retries within a token. No abort-on-logout: effect onl"
 },
 {
  "id": "R2-10",
  "severity": "major",
  "area": "accessibility",
  "file": "src/components/auth/AuthModal.tsx",
  "line": "95",
  "title": "Header login modal has no role=\"dialog\", no aria-modal, no focus trap, no Escape close",
  "evidence": "<div className=\"fixed inset-0 z-[9999] flex items-center justify-center p-4 sm:p-6 overflow-y-auto\"> — no role/aria-modal attributes anywhere in the file (grep shows only body-overflow effects at lines 29-41); focus is never moved into the modal nor trapped, so keyboard/SR users can tab into background content and Escape does not close it.",
  "suggested_fix": "Add role=\"dialog\" aria-modal=\"true\" aria-label, autofocus the first field, trap Tab within the panel, close on Escape (CartDrawer.tsx and Blog.tsx already do this).",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "AuthModal.tsx:95 is verbatim `<div className=\"fixed inset-0 z-[9999] ...\">` — grep of whole file finds zero occurrences of role=, aria-modal, or an Escape/keydown handler (uses createPortal at line 2 but no dialog semantics). No focus trap or autofocus. Evidence and comparison to CartDrawer/Blog (which do have role=\"dialog\" aria-modal + Escape) check out."
 },
 {
  "id": "R3-01",
  "severity": "major",
  "area": "JWT / refresh tokens",
  "file": "server/routes/auth.ts",
  "line": "109",
  "title": "Refresh token accepted from JSON body, and both tokens returned in JSON response — HttpOnly cookie protection undermined",
  "evidence": "const refreshToken = cookies.refreshToken || (req.body && req.body.refreshToken);\n...\nres.json({ message: \"ورود با موفقیت انجام شد\", user: {...}, ...tokens });",
  "suggested_fix": "Remove the `req.body.refreshToken` fallback and stop spreading `...tokens` into JSON responses (register/login/otp/verify/refresh all do this). Rely solely on the HttpOnly cookies; the client already uses cookie-based refresh via authFetch. Keeping tokens out of JS-readable responses makes XSS token exfiltration materially harder.",
  "src": "R3-security",
  "validation": "ADJUSTED",
  "validation_note": "Evidence verified: auth.ts:109 `cookies.refreshToken || (req.body && req.body.refreshToken)` and tokens spread into JSON at register (:58), login (:98), refresh (:137). BUT removal is NOT zero-risk: the client actively READS body tokens — AuthContext.tsx stores `data.accessToken` into localStorage at lines 86-87, 120-121, 149-150, 180-181, and src/lib/api.ts:8-9 does the same for the refresh flow;"
 },
 {
  "id": "R3-02",
  "severity": "major",
  "area": "JWT / refresh rotation",
  "file": "server/routes/auth.ts",
  "line": "17-21",
  "title": "No refresh-token rotation state or revocation: logout/compromise cannot invalidate a live refresh token (7d TTL)",
  "evidence": "const generateTokens = (userId: string) => {\n  const accessToken = jwt.sign({ userId }, env.JWT_ACCESS_SECRET, { expiresIn: \"1d\" });\n  const refreshToken = jwt.sign({ userId }, env.JWT_REFRESH_SECRET, { expiresIn: \"7d\" });\n};\n... router.post(\"/logout\", (req, res) => { clearAuthCookies(res, ...); res.json(...); });",
  "suggested_fix": "Rotation reissues a fresh pair (good) but old refresh tokens stay valid until 7d expiry — /logout only clears cookies server-side and there is no token-version/jti denylist. Add a `tokenVersion` (or jti) column on users, embed it in the refresh JWT, check+increment on logout/password change/admin password reset (server/routes/admin.ts:289 sets a new password but any stolen refresh token still works).",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "auth.ts:17-21 generateTokens signs only {userId}, no jti/tokenVersion. No tokenVersion column anywhere in server/ (grep: 0 matches). /logout (auth.ts:144-147) only clears cookies. Admin password reset (admin.ts:289-315) updates only `password` — nothing bumped, so a stolen 7d refresh token survives admin password reset. OTP reset-password path (auth.ts:184-185) same. Finding accurate."
 },
 {
  "id": "R3-03",
  "severity": "major",
  "area": "Zod validation coverage",
  "file": "server/routes/admin.ts",
  "line": "289-935",
  "title": "12 admin mutation endpoints have no Zod validate() middleware (hand-rolled, inconsistent checks)",
  "evidence": "router.put('/users/:id/password', async (req, res) => { const { newPassword } = req.body; if (!newPassword || typeof newPassword !== 'string' || newPassword.length < 6) {...}\n// also unvalidated: /users/:id/role (317), /users/:id/points (344), /products (372), /products/:id (404), /orders/:id/status (511), /coupons (593), /coupons/:code (635), /orders/:id/tracking (803), /reviews/:id/approved (841), /contact-messages/:id/status (717), /settings (935)",
  "suggested_fix": "Add Zod schemas in server/validators (adminPasswordSchema, roleSchema, pointsSchema, productUpsertSchema, orderStatusSchema, couponUpsertSchema, trackingSchema, approvedSchema, messageStatusSchema, settingsSchema) and pass to validate() like cart/orders/auth routes already do. Admin-only status lowers exploitability but parseInt()-coercion gaps (e.g. `parseInt(stockQuantity) || 0` silently zeroes stock on garbage input) are real logic bugs.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "Spot-checked 9 of 12: /users/:id/password (:294 hand-rolled length>=6), /users/:id/role (:322 allowlist), /users/:id/points (:349 Number check), POST /products (:376-390 with `parseInt(price) || 0` silently zeroing garbage), PUT /products/:id (:409+ same pattern), /orders/:id/status (:516 allowlist but statusText unvalidated), POST /coupons (:596-622 parseInt coercion), PUT /coupons/:code (:645-66"
 },
 {
  "id": "R1-05",
  "severity": "minor",
  "area": "cache-invalidation",
  "file": "server/routes/orders.ts",
  "line": "288 (also admin.ts:394-395, 427-428, 463-464, 555)",
  "title": "Stock/price changes never bust the `product:<id>` detail cache — invalidate('products') does not match 'product:<id>' keys",
  "evidence": "appCache.invalidate(\"products\");   // orders.ts after order placement / cancel\n// cache.ts:28-38: match is key.includes(pattern)\n// products.ts:258: const cacheKey = `product:${id}`;  appCache.set(cacheKey, formatted, 60);",
  "suggested_fix": "'products' is not a substring of 'product:5', so GET /api/products/:id keeps serving stale stockQuantity/price for up to 60s after an order, cancellation, or admin product edit. Invalidate('product:') (prefix) or call appCache.invalidate() with no pattern for product mutations.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified the matching logic myself: cache.ts:28-38 invalidate() uses key.includes(pattern) for string patterns. 'product:5'.includes('products') === false (the substring 'products' is not present in 'product:5'), so the detail cache key set by products.ts:258 (`product:${id}`, TTL 60s) is never busted by invalidate('products') in orders.ts:288/365 or admin.ts:394/427/463/555 — those only clear lis"
 },
 {
  "id": "R1-07",
  "severity": "minor",
  "area": "vip-points",
  "file": "server/routes/orders.ts",
  "line": "347-352 (also admin.ts:538-543)",
  "title": "VIP clawback can drive vipPoints negative",
  "evidence": "const pointsEarnedByOrder = order.status === \"processing\" ? Number(order.vipPointsEarned) || 0 : 0;\n      if (pointsEarnedByOrder > 0) {\n        await tx.update(users)\n          .set({ vipPoints: sql`${users.vipPoints} - ${pointsEarnedByOrder}` })\n          .where(eq(users.id, userId));",
  "suggested_fix": "If an admin adjusted the user's points (PUT /api/admin/users/:id/points sets an absolute value) after the order was credited, the clawback subtracts from a balance that may not contain those points, producing a negative vip_points. Clamp with SQL: set vipPoints = MAX(0, vipPoints - X) or guard with vipPoints >= X.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Code verified: orders.ts:347-352 and admin.ts:538-543 subtract pointsEarnedByOrder unconditionally (no vipPoints >= X guard, no MAX(0,...)). Admin PUT /users/:id/points (admin.ts:344-362) sets an absolute value, so an admin reset (e.g. to 0 or 50) followed by a cancellation of an older 'processing' order drives vipPoints negative. Schema has no CHECK constraint preventing it. Real."
 },
 {
  "id": "R1-08",
  "severity": "minor",
  "area": "empty-catch",
  "file": "server/bot/bale.ts",
  "line": "565, 658, 1741, 1789",
  "title": "Empty catch blocks silently swallow errors in the Bale bot",
  "evidence": "} catch {}",
  "suggested_fix": "At minimum log the swallowed error (console.warn with the method/context); silent catches hide broken notification/fallback paths.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "grep verified: empty catch blocks exist exactly at bale.ts:565, 658, 1741, 1789. All four silently swallow errors. Minor hygiene finding stands."
 },
 {
  "id": "R1-09",
  "severity": "minor",
  "area": "rate-limiting",
  "file": "server/app.ts",
  "line": "158-199",
  "title": "No dedicated rate limiter on POST /api/orders, POST /api/payment/request, /api/auth/refresh — only the general 600/15min cap",
  "evidence": "app.use(\"/api/auth/login\", authLimiter);\napp.use(\"/api/auth/register\", authLimiter);\napp.use(\"/api/auth/otp/send\", authLimiter);\napp.use(\"/api/auth/otp/verify\", authLimiter);\napp.use(\"/api/auth/reset-password\", authLimiter);\n// (payment/request, orders POST, auth/refresh absent)",
  "suggested_fix": "POST /api/payment/request hammers external bank gateways on each call and /api/auth/refresh is an unauthenticated token-oracle endpoint; give both a strict limiter (e.g. 10-20/min/IP), matching the authLimiter pattern.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified app.ts: only authLimiter (login/register/otp send+verify/reset-password, app.ts:179-183), couponLimiter (198-199), newsletterLimiter, cspReportLimiter, and the general 600/15min limiter (app.ts:165). POST /api/orders, POST /api/payment/request, and POST /api/auth/refresh have only the general cap. Severity nuance: payment/request and orders are authenticated (requireAuth), so abuse is per"
 },
 {
  "id": "R1-12",
  "severity": "minor",
  "area": "users",
  "file": "server/routes/users.ts",
  "line": "32-52",
  "title": "PUT /api/users/me can take an already-registered phone — unique-constraint violation surfaces as a generic 500",
  "evidence": "...(phone !== undefined && { phone })\n      })\n      .where(eq(users.id, userId))\n      .returning();\n\n    const { password: _, ...userWithoutPassword } = updatedUser;",
  "suggested_fix": "Pre-check `phone` uniqueness (or catch the constraint error and return 400 with a Persian message). A failed unique index currently yields a generic 500 and the profile update is silently lost.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified users.ts:32-52: PUT /me writes phone with no uniqueness pre-check and the bare catch returns generic 500 ('خطای سرور'), losing the update. On the register/login side: both schemas (validators/index.ts:110-128) DO normalize phone via z.preprocess(normalizeIranianPhone, /^09\\d{9}$/), so register's duplicate check (auth.ts:29) and login are format-consistent — but normalization is not the is"
 },
 {
  "id": "R1-13",
  "severity": "minor",
  "area": "coupons",
  "file": "server/routes/coupons.ts",
  "line": "61-66",
  "title": "Coupon validation response leaks internal usage data via raw row",
  "evidence": "res.json({\n      valid: true,\n      coupon,\n      discount,\n      finalTotal\n    });",
  "suggested_fix": "`coupon` is the raw DB row including usedCount/usageLimit/expiresAt internals. Return only {code,label,percent,amount,minTotal} plus computed discount/finalTotal.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified coupons.ts: res.json({valid:true, coupon, discount, finalTotal}) returns the raw DB row (usedCount, usageLimit, minTotal, expiresAt, active internals) to unauthenticated callers. Real info-leak/hygiene issue; minor is appropriate."
 },
 {
  "id": "R2-03",
  "severity": "minor",
  "area": "stale-closure",
  "file": "src/contexts/CartContext.tsx",
  "line": "120-135",
  "title": "addToCart computes newQty from stale `cart` closure — rapid double-clicks lose a quantity increment",
  "evidence": "const existing = cart.find(item => item.id === product.id); ... newQty = Math.min(existing.quantity + quantity, MAX_CART_QUANTITY); saveCart(cart.map(...)) — two synchronous clicks before re-render both read the same `cart`, so the second overwrites the first (qty +1 not +2).",
  "suggested_fix": "setCart(prev => ...) functional update and compute newQty inside it; sync to localStorage in an effect or inside the updater.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "CartContext.tsx:121-134: `const existing = cart.find(...)`, then `saveCart(cart.map(...))` — `cart` is the render closure; addToCart is async but the state reads happen synchronously before the await, so two clicks in the same tick both read the same `cart` and the second saveCart overwrites the first. Also `setIsCartDrawerOpen` after await doesn't matter. Fix (functional updater) is right, though"
 },
 {
  "id": "R2-04",
  "severity": "minor",
  "area": "race-condition",
  "file": "src/pages/ProductDetail.tsx",
  "line": "41-97",
  "title": "Product fetch has no cancellation/abort — fast navigation between products can render the wrong product",
  "evidence": "useEffect(() => { setLoading(true); fetch(`/api/products/${id}`)...then((data) => { setProduct(data); ... }) .catch(() => { setProduct(null); setLoading(false); }); }, [id]) — no `cancelled` flag or AbortController; a slow response for the previous id resolves last and overwrites the current product (also re-appends stale JSON-LD).",
  "suggested_fix": "Add `let cancelled = false;` + guard in then/catch with cleanup, or AbortController aborted in the effect cleanup (compare Offers.tsx which does this correctly).",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ProductDetail.tsx:40-70+: useEffect fetches `/api/products/${id}` with no `cancelled` flag or AbortController — verified lines 42-61 call setProduct unconditionally in .then. Out-of-order resolution on fast id navigation can render the wrong product; JSON-LD re-append (lines 66-70) also unconditional. Lightbox Escape handler (89-102) IS cleaned up, but the fetch isn't."
 },
 {
  "id": "R2-05",
  "severity": "minor",
  "area": "race-condition",
  "file": "src/components/HeaderSearch.tsx",
  "line": "144-164",
  "title": "Live-search fetch results not guarded against out-of-order responses",
  "evidence": "const timer = setTimeout(() => { fetch(`/api/products?search=...`).then(...setResults(data)...) }, 180); return () => clearTimeout(timer); — timer is cleaned up, but a slow in-flight fetch for an earlier query can resolve after a newer one and overwrite `results` with stale matches.",
  "suggested_fix": "Track the latest query in a ref (or use AbortController per effect run) and ignore responses whose query !== latest.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "HeaderSearch.tsx:147-162: cleanup clears only the setTimeout timer; an already-fired fetch for an earlier query can resolve after a newer query's and overwrite setResults (no latest-query ref, no AbortController). Note lines 53-68 show the codebase knows the pattern (`cancelled` flag for categories) — it's just missing here."
 },
 {
  "id": "R2-07",
  "severity": "minor",
  "area": "memory-leak",
  "file": "src/components/ChatWidget.tsx",
  "line": "90-100",
  "title": "Bot-reply setTimeout not cleared on unmount — setState after unmount if widget closes/user navigates mid-'typing'",
  "evidence": "setIsTyping(true); setTimeout(() => { ... setMessages(prev => [...prev, botMsg]); setIsTyping(false); }, 800); — no timer ref/cleanup.",
  "suggested_fix": "Keep timer in a ref and clearTimeout in the component's unmount effect.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ChatWidget.tsx:90-100: setTimeout(800) calls setMessages/setIsTyping with no timer ref or cleanup; unmounting mid-'typing' schedules a dead setState. Harmless-in-practice but real."
 },
 {
  "id": "R2-08",
  "severity": "minor",
  "area": "memory-leak",
  "file": "src/pages/Login.tsx",
  "line": "52-62",
  "title": "OTP countdown interval leaks on unmount and stacks when startCountdown is called twice",
  "evidence": "const startCountdown = (sec = 120) => { setOtpCountdown(sec); const interval = setInterval(() => { setOtpCountdown((prev) => { if (prev <= 1) { clearInterval(interval); return 0; } return prev - 1; }); }, 1000); }; — interval only self-clears via the updater; no cleanup return, and a second send-OTP creates a second concurrent interval.",
  "suggested_fix": "Hold the interval in a ref, clearInterval the previous one at the start of startCountdown, and clear it in a useEffect cleanup.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "Login.tsx:50-62 exactly as quoted: interval only self-clears inside the setState updater; startCountdown called again (line 80, on resend) creates a second concurrent interval — both tick down `otpCountdown`, doubling the decrement rate. No useEffect cleanup exists for it. This one is user-visible, arguably more than minor."
 },
 {
  "id": "R2-12",
  "severity": "minor",
  "area": "design-token-drift",
  "file": "src/components/Layout.tsx",
  "line": "17",
  "title": "App shell hardcodes dark canvas #090d16, drifting from --color-canvas-dark #0a1128",
  "evidence": "className=\"min-h-screen ... bg-[#f8fafc] dark:bg-[#090d16]\" while index.css:58 defines --color-canvas-dark: #0a1128 (and index.css:131 paints body with var(--color-canvas-dark)). Additional drift: Header.tsx:81 dark:bg-[#080d15]/90, CartDrawer.tsx:69 dark:bg-[#0c1017], LatestReviews.tsx:81 dark:bg-[#0d121c] — four different near-black surfaces vs the token.",
  "suggested_fix": "Use bg-[var(--color-canvas-light)] dark:bg-[var(--color-canvas-dark)] in Layout and consolidate the Header/CartDrawer/LatestReviews hardcoded hexes onto the surface tokens.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "Layout.tsx:17 verbatim `bg-[#f8fafc] dark:bg-[#090d16]`; index.css:58 `--color-canvas-dark: #0a1128`. Header.tsx:81 `dark:bg-[#080d15]/90`, CartDrawer.tsx:69 `dark:bg-[#0c1017]` — verified. Could not verify LatestReviews.tsx #0d121c (no such grep hit in src/components/home) — that one cited line appears stale, but the core claim (3+ hardcoded near-blacks drifting from token) stands on the three ve"
 },
 {
  "id": "R2-13",
  "severity": "minor",
  "area": "design-token-drift",
  "file": "src/components/ProductCard.tsx",
  "line": "143",
  "title": "Hardcoded text color dark:text-[#f7f8f8] duplicates --color-text-main-dark instead of referencing the token",
  "evidence": "className=\"... text-zinc-900 dark:text-[#f7f8f8] ...\" (also Products.tsx:79,92) — #f7f8f8 is the literal value of --color-text-main-dark (index.css:78), copy-pasted rather than tokenized.",
  "suggested_fix": "Replace with dark:text-[var(--color-text-main-dark)] so a future palette change propagates.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ProductCard.tsx:143 `dark:text-[#f7f8f8]` and Products.tsx:79,92 same — verified; index.css:78 `--color-text-main-dark: #f7f8f8`. Pure token drift, cosmetic."
 },
 {
  "id": "R2-14",
  "severity": "minor",
  "area": "rtl-persian",
  "file": "src/contexts/CartContext.tsx",
  "line": "125, 174",
  "title": "User-visible cart-limit toasts render English digits (۹۹ shown as 99)",
  "evidence": "addToast(`حداکثر تعداد مجاز (${MAX_CART_QUANTITY} عدد) در سبد خرید قرار دارد`, 'warning'); and `حداکثر تعداد مجاز ${MAX_CART_QUANTITY} عدد می‌باشد` — MAX_CART_QUANTITY (constants.ts:13 = 99) is interpolated raw, unlike the rest of the UI which uses toPersianDigits.",
  "suggested_fix": "Wrap with toPersianDigits(MAX_CART_QUANTITY) — import from lib/utils like other components.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "Exact strings verified: CartContext.tsx:125 `addToast(`حداکثر تعداد مجاز (${MAX_CART_QUANTITY} عدد) ...`)` and :174 `` `حداکثر تعداد مجاز ${MAX_CART_QUANTITY} عدد می‌باشد` `` — MAX interpolated raw; constants.ts:13 `MAX_CART_QUANTITY = 99`, so toasts render '(99 عدد)' with English digits in a Persian RTL UI. No toPersianDigits import in CartContext."
 },
 {
  "id": "R2-16",
  "severity": "minor",
  "area": "race-condition",
  "file": "src/contexts/CartContext.tsx",
  "line": "137-148",
  "title": "Server cart mutation responses ignored — local optimistic state can disagree with server (stock-limited qty)",
  "evidence": "await authFetch('/api/cart', { method: 'POST', ... body: JSON.stringify({ productId: product.id, quantity }) }); — response body is never read; the server clamps quantity to stock/max-10 (per API spec) but the local cart keeps the requested quantity, so checkout totals can diverge until the next full fetch.",
  "suggested_fix": "Reconcile from the POST/PUT response (or refetch cart) when the server-returned item differs from the optimistic one.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "CartContext.tsx:139-148 (and 159-167, 181-190): POST/PUT/DELETE responses awaited but body never read; local state keeps requested quantity. Server-side clamping (stock/max-10) can silently diverge from optimistic local cart until next full fetch. Accurate; note the merge fetch (R2-02) is the only reconciliation path."
 },
 {
  "id": "R2-17",
  "severity": "minor",
  "area": "re-render-hotspot",
  "file": "src/hooks/useProductFilters.ts",
  "line": "28, 118",
  "title": "Client filter cache never invalidated — stale prices/stock served all session after any admin change",
  "evidence": "const clientFilterCache = new Map<string, {...}>(); (module scope) ... clientFilterCache.set(cacheKey, { products: data, total: count, totalPages: pages }); — entries live for the whole SPA session; Compare.tsx explicitly refetches for this reason, but the Products page serves cached rows indefinitely.",
  "suggested_fix": "Add a TTL (e.g. 60s) or bust the cache on visibilitychange/focus.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "useProductFilters.ts:21 module-scope `clientFilterCache = new Map(...)`, :95 serves if has(cacheKey), :118 sets — no TTL, no invalidation anywhere in file; entries live for the SPA session. Verified."
 },
 {
  "id": "R3-04",
  "severity": "minor",
  "area": "Zod validation coverage",
  "file": "server/routes/contact.ts",
  "line": "36-86",
  "title": "POST /api/contact and POST /api/contact/newsletter validated manually, not with shared Zod schemas",
  "evidence": "router.post(\"/\", async (req, res) => {\n  const { name, email, phone, subject, message } = req.body;\n  if (!name || !email || !message) { return res.status(400)... }\n  ... String(name).slice(0, 200)",
  "suggested_fix": "Use validate(contactSchema)/validate(newsletterSchema) for consistency; also add an email format check to /api/contact (currently any non-empty string passes as email) and a dedicated contact rate limiter — only the general 600/15min limiter applies, so message-table flooding at ~40/min is possible.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "contact.ts:36-41 manual `if (!name || !email || !message)` checks, no email format validation on /api/contact (newsletter at :84 does have a regex — contrast is real). No dedicated limiter on /api/contact in app.ts (only general 600/15min at :158-165); auth limiter list (:179-183) does not include contact. Length clamping at :47-51 limits abuse but flood risk stands."
 },
 {
  "id": "R3-05",
  "severity": "minor",
  "area": "Headers / CSP",
  "file": "server/app.ts",
  "line": "88",
  "title": "CSP scriptSrc includes 'unsafe-inline' and imgSrc allows plain http:",
  "evidence": "scriptSrc: [\"'self'\", \"'unsafe-inline'\"],\n...\nimgSrc: [\"'self'\", \"data:\", \"https:\", \"http:\"],",
  "suggested_fix": "Replace 'unsafe-inline' with nonce- or hash-based script policy (Vite bundles are external files; inline usage is small). Drop `http:` from imgSrc to avoid mixed-content loading of attacker-influenced product image URLs. Weakens XSS containment if an injection ever lands.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "app.ts:88 `scriptSrc: [\"'self'\", \"'unsafe-inline'\"]` and :91 `imgSrc: [\"'self'\", \"data:\", \"https:\", \"http:\"]`. Both exact. CSP only active in production (env-gated at :84) which is the environment that matters."
 },
 {
  "id": "R3-06",
  "severity": "minor",
  "area": "Information disclosure",
  "file": "server/routes/admin.ts",
  "line": "337",
  "title": "PUT /api/admin/users/:id/role returns the full DB row including the bcrypt password hash",
  "evidence": "const [updated] = await db.update(users).set({ role }).where(eq(users.id, id)).returning();\n...\nres.json({ message: 'User role updated successfully', user: updated });",
  "suggested_fix": "Destructure out `password` before responding (`const { password, ...safe } = updated`), matching the pattern used in GET /users (line 275-278). Admin-only, but the hash then sits in browser devtools/proxies/logs unnecessarily.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "admin.ts:332-337: `const [updated] = await db.update(users).set({ role })...returning(); ... res.json({ ..., user: updated })` — updated is the full row including password hash. Contrast with GET /users (:275-278) which strips password. Exact as reported."
 },
 {
  "id": "R4-01",
  "severity": "minor",
  "area": "test-coverage",
  "file": "server/routes/upload.ts",
  "line": "1",
  "title": "No test file covers /api/admin upload route",
  "evidence": "grep -rl upload tests/ -> only tests/unit/bale-bot.test.ts (unrelated); no tests/api/upload*.test.ts exists; ls tests/api shows 26 suites, none target upload.ts",
  "suggested_fix": "Add tests/api/upload.test.ts covering auth/admin gating, file-type and size validation, and rejection paths.",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R4-02",
  "severity": "minor",
  "area": "test-coverage",
  "file": "server/routes/coupons-active.ts",
  "line": "1",
  "title": "No test file targets the coupons-active route",
  "evidence": "grep -rl 'coupons-active' tests/ -> empty output; coupons.test.ts covers /api/coupons/validate and admin coupon CRUD but not coupons-active.ts",
  "suggested_fix": "Add a test for GET active-coupons listing (active/expiry filtering, no leaking of discountValue internals if public).",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R4-03",
  "severity": "minor",
  "area": "test-coverage",
  "file": "server/routes/blog.ts",
  "line": "1",
  "title": "Blog API route has no direct API test",
  "evidence": "grep -rl blog tests/ -> tests/unit/blog-json-ld.test.ts, tests/api/sitemap.test.ts, tests/api/breadcrumbs.test.ts (JSON-LD/sitemap/breadcrumb only); no tests/api/blog.test.ts exercising blog.ts endpoints directly",
  "suggested_fix": "Add tests/api/blog.test.ts for blog list/detail endpoints (pagination, slug lookup, 404 path).",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R1-06",
  "severity": "improvement",
  "area": "cart",
  "file": "server/routes/cart.ts",
  "line": "90-105",
  "title": "PUT /api/cart/:id accepts any quantity — max-10 and stock guards are not enforced on update",
  "evidence": "const { quantity } = req.body;\n    try {\n    await db.update(cartItems)\n      .set({ quantity })\n      .where(and(eq(cartItems.userId, userId), eq(cartItems.productId, productId)));",
  "suggested_fix": "Validate 1 <= quantity <= 10 and quantity <= product.stockQuantity (as POST / does). Currently a client can set quantity=999999; the invariant is only rescued later by the order-transaction stock guard.",
  "src": "R1-backend",
  "validation": "ADJUSTED",
  "validation_note": "Half the evidence is wrong. cart.ts:90 has validate(updateCartItemSchema) and validators/index.ts:159-163 defines it as quantity: z.number().int().positive().max(10) — the max-10 guard IS enforced on PUT (finding's title and evidence claiming 'max-10 not enforced' is a misread; the cited evidence snippet omitted the validate middleware). What remains real: no stockQuantity check on update (POST / "
 },
 {
  "id": "R1-10",
  "severity": "improvement",
  "area": "n+1/performance",
  "file": "server/routes/admin.ts",
  "line": "139-146",
  "title": "GET /api/admin/analytics loads every order+items, every user and every product into JS memory",
  "evidence": "const allOrders = await db.query.orders.findMany({\n      with: { items: true }\n    });\n\n    const allUsers = await db.select().from(users);\n    const allProducts = await db.select().from(products);",
  "suggested_fix": "Category sales and top-selling products are expressible as SQL GROUP BY joins over order_items; the sales trend can filter orders to the 14-day window instead of scanning all history. Same full-table pattern remains in GET /api/admin/orders when page/limit are omitted (admin.ts:500-505).",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified admin.ts:139-146: db.query.orders.findMany({with:{items:true}}) loads every order + items, plus full users and products table scans, all aggregated in JS. Real full-table pattern; improvement-level performance note stands."
 },
 {
  "id": "R1-11",
  "severity": "improvement",
  "area": "n+1/performance",
  "file": "server/routes/orders.ts",
  "line": "229-262",
  "title": "Order creation inserts order items and updates stock one row at a time inside the transaction",
  "evidence": "for (const item of finalItems) {\n        await tx.insert(orderItems).values({...});\n        const updatedRows = await tx.update(products)... .returning({ id: products.id });\n        ...\n      }",
  "suggested_fix": "Batch-insert orderItems with a single tx.insert().values(items[]). Keep the per-item guarded UPDATE for the row-count oversell assert, but consider batching the inserts; minor latency win per order, matters most on PG round-trips.",
  "src": "R1-backend",
  "validation": "CONFIRMED",
  "validation_note": "Verified orders.ts:229-262: per-item await tx.insert(orderItems) loop inside the transaction (stock update is per-item by necessity for the RETURNING oversell guard, which is correct and should stay). On SQLite the loop is cheap (in-process); the win is on PG round-trips. Improvement-level, accurate as stated."
 },
 {
  "id": "R2-06",
  "severity": "improvement",
  "area": "memory-leak",
  "file": "src/contexts/ToastContext.tsx",
  "line": "23-27",
  "title": "Toast removal setTimeout never cleared — state update after unmount / no cleanup on provider teardown",
  "evidence": "setTimeout(() => { setToasts((prev) => prev.filter((t) => t.id !== id)); }, 3000); — fire-and-forget timer, not stored or cleared. Harmless for the app-root provider but each toast schedules an untracked timer.",
  "suggested_fix": "Store timer ids in a ref Map and clear on unmount; low impact since ToastProvider lives at root.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ToastContext.tsx:23-25: fire-and-forget setTimeout(3000) with setToasts filter, never stored/cleared. Real but negligible: provider is app-root, functional update is safe after unmount in React 19. Finding itself admits 'low impact'."
 },
 {
  "id": "R2-09",
  "severity": "improvement",
  "area": "memory-leak",
  "file": "src/components/ProductCard.tsx",
  "line": "30",
  "title": "setTimeout(() => setAdded(false), 2000) not cleaned up on unmount (same in VipClubBanner.tsx:26, static/Blog.tsx:438)",
  "evidence": "addToCart(product); setAdded(true); ... setTimeout(() => setAdded(false), 2000); — a card unmounted within 2s of add-to-cart schedules a setState on a dead component.",
  "suggested_fix": "Clear the timeout in a useEffect cleanup (timer ref), or use a small useTimedFlag hook shared by these three sites.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ProductCard.tsx:30 `setTimeout(() => setAdded(false), 2000)` no cleanup — verified. VipClubBanner.tsx:26 same pattern — verified. static/Blog.tsx:438 same — verified. All three real; impact negligible (one harmless setState on unmounted component)."
 },
 {
  "id": "R2-11",
  "severity": "improvement",
  "area": "accessibility",
  "file": "src/components/cart/CartDrawer.tsx",
  "line": "50-67",
  "title": "Cart drawer has role=dialog/aria-modal but no focus management",
  "evidence": "<div className=\"fixed inset-0 z-50 overflow-hidden\" role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"cart-drawer-title\"> — Escape close exists (lines 22-31) but focus is neither moved into the drawer on open nor returned to the trigger on close, and Tab can escape into the page behind the modal.",
  "suggested_fix": "Focus the close button on open, implement a Tab cycle (or use <dialog>/inert on background), and restore focus to the opener on close.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "CartDrawer.tsx:50 has role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"cart-drawer-title\"; Escape close at lines 19-28. No focus() call anywhere in the file — focus is never moved in nor restored. Accurate as stated."
 },
 {
  "id": "R2-15",
  "severity": "improvement",
  "area": "contrast",
  "file": "src/index.css",
  "line": "33-36",
  "title": "Brand CTA orange (#F47C20) with white text is 2.7:1 — documented but still used widely on buttons/badges (AA fail for normal text on Obsidian/light canvases)",
  "evidence": "index.css comment: \"primary-container #F47C20 = CTA fills (white text 2.7:1, large/bold only)\" — yet bg-orange-500 text-white appears throughout (e.g. ProductFilterSidebar.tsx:192,192 'bg-orange-500 text-white', :265 'bg-orange-500 text-white border-orange-500') for small filter chips/labels, not only large/bold text.",
  "suggested_fix": "Use --color-primary-500 #d94e06 (4.16:1) or --color-primary-700 #994700 (6.12:1) for small white-on-orange text; reserve #F47C20 for large/bold or non-text elements.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "Recomputed: WCAG relative-luminance ratio of #F47C20 on #FFFFFF = 2.709:1 ≈ the documented 2.7:1 (index.css:33 comment verified verbatim). Suggested alternates recompute: #d94e06 → 4.16:1 (matches comment), #994700 → 6.45:1 (comment says 6.12:1 — slightly off but direction correct, both pass AA). Usage claim verified: ProductFilterSidebar.tsx:192,265 (and :368, ProductSortHeader.tsx:61 at text-[10"
 },
 {
  "id": "R2-18",
  "severity": "improvement",
  "area": "re-render-hotspot",
  "file": "src/contexts/ToastContext.tsx",
  "line": "43-53",
  "title": "Context value object recreated every render — all addToast consumers re-render on every toast add/remove",
  "evidence": "<ToastContext.Provider value={{ addToast }}> — a new object identity each render of ToastProvider; with ~35 files consuming toasts, each toast's 3s expiry re-renders every subscribed subtree (CartContext/AuthContext providers and their children are descendants).",
  "suggested_fix": "useMemo the value ({ addToast }) — addToast is already useCallback'd, so the memo makes the value stable and eliminates cascade re-renders.",
  "src": "R2-frontend",
  "validation": "CONFIRMED",
  "validation_note": "ToastContext.tsx:29 `<ToastContext.Provider value={{ addToast }}>` — new object identity every render; addToast IS useCallback'd (line 20) but the wrapping object is not memoized, so every toast add/remove re-renders all consumers. One-line useMemo fix is valid. Overlaps thematically with R2-06 (same file) but is a distinct issue — not a duplicate."
 },
 {
  "id": "R3-07",
  "severity": "improvement",
  "area": "Payment verify",
  "file": "server/routes/payment.ts",
  "line": "173-177",
  "title": "Unauthenticated GET /api/payment/verify lets the caller pick the verification provider via query param",
  "evidence": "const explicitProvider = req.query.provider as any;\nconst verifyResult = await paymentRouter.verifyPayment({ authority, status, amountTomans: order.total, orderId: order.id }, explicitProvider);",
  "suggested_fix": "Verify against the provider recorded on the order (the one that issued the authority) instead of trusting `req.query.provider`; a caller could steer verification to a gateway where the authority/amount check differs. Low severity because amount+authority are still checked, but it removes a free parameter.",
  "src": "R3-security",
  "validation": "ADJUSTED",
  "validation_note": "payment.ts:169-175 confirmed passing req.query.provider. PaymentFailoverRouter.verifyPayment (:117-126): explicitProvider==='saman' OR authority.startsWith('SEP_') → secondary; otherwise primary. So explicitProvider does change routing — but only in the failure-safe direction: steering a ZP authority to Saman makes Saman verify an unknown authority (fails); steering a SEP authority to primary make"
 },
 {
  "id": "R3-08",
  "severity": "improvement",
  "area": "Env / secrets hardening",
  "file": "server/env.ts",
  "line": "18-19",
  "title": "JWT secrets only require 10 chars and are only blocklisted against 6 known placeholder strings",
  "evidence": "JWT_ACCESS_SECRET: z.string().min(10, \"JWT_ACCESS_SECRET must be at least 10 characters long\"),\nJWT_REFRESH_SECRET: z.string().min(10, ...),\n// + INSECURE_DEFAULT_SECRETS literal list check only in production",
  "suggested_fix": "Require min 32 chars and reject low-entropy secrets (e.g. entropy/length heuristic) rather than a fixed blocklist. Also assert JWT_ACCESS_SECRET !== JWT_REFRESH_SECRET (currently not checked — reusing one value for both silently passes validation). .env on disk contains all expected keys (PORT, DATABASE_URL, JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, ZARINPAL_MERCHANT_ID, ZARINPAL_SANDBOX, GEMINI_API_KEY, APP_URL, SMS_API_KEY, SMS_PROVIDER, SMS_TEMPLATE_ID, BALE_BOT_TOKEN, BALE_ADMIN_CHAT_IDS) — values not inspected.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "env.ts:18-19 min(10); INSECURE_DEFAULT_SECRETS list at :5-13 has exactly 6 entries, checked only for production (:42-57). No min-length check between JWT_ACCESS_SECRET and JWT_REFRESH_SECRET (they may be identical). All as reported."
 },
 {
  "id": "R3-09",
  "severity": "improvement",
  "area": "Cookies",
  "file": "server/utils/cookies.ts",
  "line": "30-31",
  "title": "Auth cookies lack __Host- prefix and explicit Domain scoping relies on default",
  "evidence": "res.append(\"Set-Cookie\", `accessToken=${accessToken}; Path=/; HttpOnly; SameSite=${sameSite}; Max-Age=${accessMaxAge}${secure}`);",
  "suggested_fix": "Rename to `__Host-accessToken`/`__Host-refreshToken` (requires Secure, Path=/, no Domain — all already true in prod) to pin cookies against subdomain-injection. Flags themselves are correct (HttpOnly, Secure in prod, SameSite=Strict in prod / Lax in dev).",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "cookies.ts:30-31 set plain `accessToken=`/`refreshToken=` names, no __Host- prefix; flags HttpOnly/Path=//Secure(prod)/SameSite=Strict(prod) all correct. Client compatibility is fine: client never reads auth cookies (HttpOnly; all fetches use credentials:'include'; api.ts refresh flow reads only JSON body, not document.cookie), and server-side parseCookies/set/clear all use the same names, so a re"
 },
 {
  "id": "R3-10",
  "severity": "improvement",
  "area": "Rate limiting",
  "file": "server/app.ts",
  "line": "161",
  "title": "All limiters skipped only in test, but general limiter keying trusts a single proxy hop — consider keying login limiter by IP+phone",
  "evidence": "const limiter = rateLimit({ windowMs: 15*60*1000, max: 600, skip: () => process.env.NODE_ENV === \"test\" || env.NODE_ENV === \"test\", ... });\napp.set(\"trust proxy\", 1);",
  "suggested_fix": "Distributed low-and-slow credential stuffing stays under 5 req/min/IP. Add a secondary per-account counter on /api/auth/login (keyGenerator including req.body.phone) so a botnet rotating IPs still gets throttled per victim phone number.",
  "src": "R3-security",
  "validation": "CONFIRMED",
  "validation_note": "app.ts:35 `app.set(\"trust proxy\", 1)`, :161 skip only in test, authLimiter (:168-183) 5/min keyed by IP only, mounted on login/register/otp/reset-password. No per-account keying anywhere. Suggestion is valid; severity improvement is right since it's a hardening measure against distributed low-and-slow stuffing."
 },
 {
  "id": "R4-04",
  "severity": "improvement",
  "area": "e2e-selectors",
  "file": "e2e/deep-coverage.spec.ts",
  "line": "68",
  "title": "Fragile add-to-cart selector based on generic button+text filter",
  "evidence": "e2e/deep-coverage.spec.ts:68 uses page.locator('main button:not([disabled])').filter({ hasText: /افزودن به سبد خرید/ }).first(); ProductCard.tsx renders the text but no stable data-testid, so DOM-order changes (e.g. multiple cards) could pick the wrong button",
  "suggested_fix": "Add data-testid='add-to-cart' to ProductCard/ProductDetail buttons and target that in the spec.",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R4-05",
  "severity": "improvement",
  "area": "e2e-selectors",
  "file": "e2e/deep-coverage.spec.ts",
  "line": "194",
  "title": "Checkout submit located via partial aria-label match",
  "evidence": "e2e/deep-coverage.spec.ts:194 uses page.locator('[aria-label*=\"ثبت نهایی سفارش\"]'); actual element is CheckoutOrderSummary.tsx:111 aria-label=\"ثبت نهایی سفارش و انتقال به درگاه پرداخت امن\" — works today via substring match but is positional/linguistically fragile",
  "suggested_fix": "Add data-testid='checkout-submit' to CheckoutOrderSummary submit button and use it.",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R4-GAP",
  "severity": "improvement",
  "area": "test-coverage",
  "file": "tests/",
  "line": "-",
  "title": "Coverage gap: e2e specs (deep-coverage, real-user, store) contain no broken selectors found: aria-label/title/role anchors verified ag",
  "evidence": "e2e specs (deep-coverage, real-user, store) contain no broken selectors found: aria-label/title/role anchors verified against src/components/profile/AddressBookTab.tsx:185/196 and CheckoutOrderSummary.tsx:111; flagged items R4-04/R4-05 are fragility improvements, not breakage",
  "suggested_fix": "Add dedicated API test suite.",
  "src": "R4-qa",
  "validation": "unvalidated"
 },
 {
  "id": "R1-02",
  "severity": "minor",
  "area": "payment/vip-points",
  "file": "server/routes/payment.ts",
  "line": "107-121",
  "title": "markOrderPaid double-credits VIP points under concurrent verify callbacks on PostgreSQL",
  "evidence": "const currentOrder = currentOrderList[0];\n    if (!currentOrder || currentOrder.status !== 'pending_payment') return;\n    await tx.update(orders)\n      .set({ status: 'processing', ... })\n      .where(eq(orders.id, orderId));\n    if (order.vipPointsEarned && order.vipPointsEarned > 0 && order.userId) {\n      await tx.update(users).set({ vipPoints: sql`${users.vipPoints} + ${order.vipPointsEarned}` })...",
  "suggested_fix": "On PG (READ COMMITTED) two concurrent /verify callbacks can both read status='pending_payment' and both credit points. Make the transition atomic: UPDATE orders SET status='processing' WHERE id=? AND status='pending_payment' RETURNING, and only credit points when a row was returned (same pattern as the oversell fix in orders.ts).",
  "src": "R1-backend",
  "validation": "ADJUSTED",
  "validation_note": "Code evidence is real (payment.ts:107-121 markOrderPaid does check-then-act: read status at line 108-110, then unconditional update at 111-117, then credit at 118-120). BUT the finding is PG-only, not exploitable in current prod. db/index.ts:302-315 wraps better-sqlite3 transactions in a promise-chain mutex (runQueued/txChain): every db.transaction is queued and executed one at a time on the singl"
 },
 {
  "id": "R1-03",
  "severity": "minor",
  "area": "orders/cancel",
  "file": "server/routes/orders.ts",
  "line": "326-363",
  "title": "User cancel: check-then-act status guard is not atomic — concurrent double-cancel double-restocks and double-refunds on PG",
  "evidence": "const order = orderList[0];\n      ...\n      if (order.status !== \"pending_payment\" && order.status !== \"processing\") {\n        throw { status: 400, ... };\n      }\n      await restockItemsAndRefundPoints(tx, orderId, userId, order.vipPointsUsed);\n      ...\n      await tx.update(orders).set({ status: \"cancelled\", ... }).where(eq(orders.id, orderId));",
  "suggested_fix": "Perform the status flip first with an atomic predicate: UPDATE orders SET status='cancelled' WHERE id=? AND status IN ('pending_payment','processing') RETURNING; if no row, abort. Then restock/refund once. Same defect exists in the cancel branch of PUT /api/admin/orders/:id/status (admin.ts:526-550).",
  "src": "R1-backend",
  "validation": "ADJUSTED",
  "validation_note": "Code is real: orders.ts:326-359 check-then-act inside tx (status read at 327-339, restock at 343, status flip at 354-359), and the admin cancel branch admin.ts:523-553 has the identical shape. Same dialect split as R1-02: on SQLite the db/index.ts txChain mutex serializes the whole transaction, so double-cancel/double-restock is impossible in current prod; on PG (READ COMMITTED, no row lock betwee"
 },
 {
  "id": "R1-04",
  "severity": "minor",
  "area": "coupons",
  "file": "server/routes/orders.ts",
  "line": "153-165, 271-276",
  "title": "Coupon usage cap is TOCTOU on PostgreSQL — concurrent orders can exceed usageLimit",
  "evidence": "if (coupon.usageLimit != null && (coupon.usedCount ?? 0) >= coupon.usageLimit) {\n          throw new Error(\"ظرفیت استفاده از این کد تخفیف تکمیل شده است\");\n        }\n        ...\n        await tx.update(coupons)\n          .set({ usedCount: sql`COALESCE(${coupons.usedCount}, 0) + 1` })\n          .where(eq(coupons.code, usedCouponCode));",
  "suggested_fix": "Apply the same atomic-predicate pattern used for stock: UPDATE coupons SET used_count = used_count + 1 WHERE code = ? AND (usage_limit IS NULL OR used_count < usage_limit) RETURNING code; if 0 rows, reject the coupon. The read-only check at line 154 is racy on PG.",
  "src": "R1-backend",
  "validation": "ADJUSTED",
  "validation_note": "Code is real: orders.ts:154 read-only usage-cap check, orders.ts:272-276 unconditional usedCount increment — classic TOCTOU. Dialect analysis: on SQLite prod the db/index.ts transaction mutex (lines 302-315) serializes the entire order-creation transaction, so the check and increment cannot interleave with another order's transaction — the cap cannot be exceeded on better-sqlite3 (single connectio"
 }
]