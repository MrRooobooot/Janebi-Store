{
  "member": "fixer-server-w4",
  "fixed": [
    "R3-02: Refresh-token revocation via tokenVersion — new server/routes/tokenVersion.ts (in-memory Map<userId,version>, no migration); version embedded in refresh JWT at sign time (auth.ts generateTokens); /auth/refresh rejects stale versions with 401 + clearAuthCookies; version bumped on POST /auth/logout (verifies cookie, bumps for that userId), admin password reset (admin.ts PUT /users/:id/password) and OTP reset-password (auth.ts /reset-password)",
    "R3-03: 12 admin endpoints wired to Zod validate() — new schemas in server/validators/index.ts: adminPasswordSchema, roleSchema, pointsSchema, productCreateSchema/productUpsertSchema, orderStatusSchema, couponCreateSchema/couponUpsertSchema, trackingSchema, approvedSchema, messageStatusSchema, settingsSchema. Role allowlist, min-lengths and required-field semantics preserved; numeric coercion strict (digit-string -> Number, else reject — no parseInt()||0 zeroing). PUT variants accept partial bodies (404 paths preserved); POST variants require core fields (400 on missing)",
    "R1-07: VIP clawback can no longer go negative — orders.ts (:id/cancel) and admin.ts (PUT /orders/:id/status cancel branch) deduct earned points with conditional UPDATE ... WHERE vipPoints >= X (clamped at 0)",
    "R1-08: bale.ts empty catches at 565/658/1741/1789 now log console.warn with context (reply_markup fallback failure, answerCallbackQuery failure, wait-message deletion failures on both success and error paths)",
    "R1-09: app.ts — dedicated strictActionLimiter (20/min per IP, NODE_ENV=test skip) mounted on /api/orders, /api/payment/request, /api/auth/refresh",
    "R1-13: coupons.ts /validate no longer leaks the raw coupon row — returns only {code,label,percent,amount,minTotal} plus computed discount/finalTotal (client src/hooks/useCartSummary.ts reads exactly these fields; usageLimit/usedCount omitted)",
    "R3-04: contact.ts POST / and /newsletter now go through validate() with contactSchema (email format + Persian-digit normalization) and newsletterSchema; required-field presence check kept in the handler to preserve the legacy flat {error: Persian string} shape the client renders; contactLimiter (10/15min, NODE_ENV=test skip) mounted on /api/contact in app.ts",
    "R3-07: payment.ts /verify no longer trusts req.query.provider — gateway routing is by authority prefix only (SEP_ => saman, else zarinpal) inside PaymentFailoverRouter",
    "infra: vitest.config.ts sets NODE_ENV=test for the suite so rate-limit skips work under tests (also protects future strict limiters)"
  ],
  "skipped": [],
  "verify": {
    "tsc": "npx tsc --noEmit — clean (exit 0)",
    "tests": "npm test — 55 files passed, 399 passed / 5 skipped / 0 failed (404 total); one transient SQLITE_BUSY flake in adversarial_challenge 2.5 on first run, clean on rerun and in isolation",
    "targeted": "tests/api/contact.test.ts, tests/api/admin.test.ts, tests/api/admin-hardening.test.ts, tests/unit/phase1-foundation.test.ts — 42/42 passed",
    "committed": false,
    "filesChanged": [
      "server/routes/tokenVersion.ts (new)",
      "server/validators/index.ts",
      "server/routes/auth.ts",
      "server/routes/admin.ts",
      "server/routes/orders.ts",
      "server/routes/coupons.ts",
      "server/routes/contact.ts",
      "server/routes/payment.ts",
      "server/bot/bale.ts",
      "server/app.ts",
      "server/middleware/validate.ts (reverted to original structured error envelope — untouched net)",
      "vitest.config.ts"
    ]
  }
}
