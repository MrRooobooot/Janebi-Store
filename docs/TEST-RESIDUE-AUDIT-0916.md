# TEST-RESIDUE-AUDIT-0916 — بازرسی کامل باقی‌ماندهٔ داده‌های تستی روی prod

**تاریخ:** ۱۴۰۵/۰۶/۲۵ · **روش:** اسکن فقط-خواندنی همهٔ ۱۵ جدول + بررسی دارایی‌های استاتیک + لینک‌های داخلی بلاگ. اسکریپت‌ها: `.hermes/imports/audit-residue2.cjs` (اسکیماتیک)، `audit-residue3.cjs` (لینک/دارایی)، `cleanup-residue.cjs` (پاک‌سازی)، `cleanup-assets.sh`.

## ۱) پاک‌سازی‌شده (انجام شد)

| بخش | قبل | نتیجه | جزئیات |
|-----|-----|-------|--------|
| `contact_messages` | ۴۴۰ | **۰** | ۴۳۹ پیام فیکسچر E2E (`E2E Tester` ۱۴۴، `علی حسینی` ۱۴۳، `رضا محمدی` ۱۴۲، `a` ۱۰) + ۱ پیام «تست کوتاه» با بدنهٔ «این یک پیام آزمایشی است» — هیچ پیام واقعی مشتری وجود ندارد |
| `newsletter_subscribers` | ۲ | **۰** | `test.user+janebi@example.com` و `phone:09123456789` |
| `orders` | ۲ | **۰** | هر دو **لغو‌شده** و بدون آیتم (بعد از حذف ۲ کالای بازمانده) — سفارش‌های تست حساب مالک |
| `order_items` | ۰ | ۰ | — |
| `cart_items` / `wishlist_items` / `addresses` / `reviews` / `product_features` | ۰ | ۰ | تمیز بودند (۰ ردیف) |
| `store_settings.heroSlide1Link` | `/products?category=هولدر و پایه` (دستهٔ **ناموجود**) | `/products?category=هولدر و نگهدارنده` | باگ لینک شکستهٔ CTA هیرو |
| دارایی‌های استاتیک | ۵۰۰ فایل یتیم (۲×۲۵۰) | **۰** | عکس‌های کالاهای تستی `p-*`/`dk-*` + فایل‌های زبالهٔ macOS `._*`؛ روی هر دو مسیر docroot (nginx=`dist/`, کانتینر=`public/`). فضای آزادشده ۲×۷۲MB |
| آرشیو محلی | — | — | ۲۵۰ عکس تستی به `.hermes/archive/test-images-0916/` منتقل شد (۷۲MB، قابل بازگردانی) |

**اثبات:** `PRAGMA integrity_check = ok`، `foreign_key_check = 0`؛ `products = 38`، `orders = 0`، `contact_messages = 0`، `newsletter = 0`. زنده: `/images/products/p-5505.jpg` ⇒ **۴۰۴** و `/images/products/ear-eh344.webp` ⇒ **۲۰۰**؛ `/api/products` ⇒ ۳۸؛ پروب Chromium+WebKit روی `/`, `/products`, دسته: `broken: 0`, `pageerror: 0`.

## ۲) سالم — نیازی به پاک‌سازی ندارد

- `users` = ۳ (همه `admin` با نام واقعی: آیدین نعمتی، معصومه کفاش، علی محمددوست). هیچ کاربر تستی/مشتری جعلی وجود ندارد.
- `blog_posts` = ۲۳، همه `published` با محتوای راهنمای واقعی؛ **صفر لینک شکستهٔ داخلی** (بررسی `/product/<id>` و `category=` داخل بدنهٔ همهٔ پست‌ها). چند پست به کالاهای خارج از موجودی اشاره می‌کنند (متن محتوایی است، لینک مرده ندارد).
- `audit_logs` = ۴۰۷ — ردّ حسابرسی، **باید بماند**.
- `public/products/*.svg` (۱۴ فایل) — همه ارجاع دارند: `hld-13.svg` عکس کالای باسئوس، بقیه در `blog_posts.image` و لیست جانگهدار عکس در پنل ادمین (`src/pages/admin/Products.tsx`).

## ۳) نیازمند تصمیم کاربر

1. **کوپن‌ها (۴):** `WELCOME10` (۱۰٪، `used_count=1`) همان کوپنی است که در نوار اعلان سایت تبلیغ می‌شود ⇒ بماند. `OFF20` (۲۰٪)، `SUMMER30` (۳۰٪ جشنوارهٔ تابستانه — فصلی و گذشته)، `JANEBI100` (۱۰۰٬۰۰۰ تومانی) الگوی seed هستند، نه کمپین مصوب ⇒ حذف یا تعیین شرط/انقضا؟
2. **CTA اسلاید دوم هیرو:** متن `heroSlide2ButtonText` («انتخاب قاب و محافظ صفحه») در `src/lib/constants.ts` سخت‌کد است و به دسته‌ای با **۰ کالا** می‌رود (اسلاید اول از `store_settings` و اکنون درست است). گزینه‌ها: (الف) تغییر به «هولدر و نگهدارنده»، (ب) مخفی‌کردن اسلاید دوم تا رسیدن موجودی آن بخش، (ج) داده‌محور کردن هر دو اسلاید.
3. **باگ مسیر تصویر در ربات بله:** `server/bot/bale.ts` در ۳ جا `/placeholder-product.svg` را صدا می‌زند که **۴۰۴** است (فایل واقعی: `/images/placeholder-product.svg`) ⇒ در پیام‌هایی که کالا عکس ندارد، تصویر شکسته می‌شود. یک‌خطی + دیپلوی.
4. **دیتابیس توسعهٔ محلی (`data/janebi.db`):** باقی‌ماندهٔ تستی دارد — ۲۳۱۷ پیام تماس (از اجراهای مکرر E2E)، ۱۴ کالا، ۱ هولدر، ۲ نظر، ۳ کاربر؛ `data/janebi.e2e.db` هم جداست. پاک‌سازی/جایگزینی با اسنپ‌شات prod؟ (توجه: هر اجرای `npm run verify` دوباره فیکسچر می‌سازد.)
