# گزارش روزانه — ۱۴۰۵/۰۶/۲۲ (2026-09-13)

۱۷ کامیت · `main == origin/main` · BUILD_INFO پرود = `c04b0ff` · گیت‌ها سبز.

## ۱) شبه‌شب — یکنواخت‌سازی هدر پنل ادمین
- `8af464e` refactor(admin): کامپوننت `PageHeader` (آیکون + عنوان + زیرعنوان) و مهاجرت هر ۱۱ صفحهٔ ادمین از ۳ واریانت دریفت‌شده.
- `32bb4f3` مستندسازی در ledger.

## ۲) چرخش C — فرم‌ها و میکرواینترکشن‌ها
- probe زندهٔ prod (DOM + pixel + vision؛ لایت/دارک × 1280/390).
- `9afc76d` چهار فیکس ریشه‌ای: `noValidate` روی ۴ فرم (تولتیپ انگلیسی مرورگر)، پیام type فارسی روی `z.number()` کوپن (نشت «Invalid input: expected number, received null») + گارد `Number.isFinite`، توست `bottom-20 lg:bottom-4 z-[60]` (روی نوار پایین موبایل)، حذف ارائهٔ دوبل خطای کوپن.
- `3753c62` ledger: ۴ فیکس + ۴ wontfix با شاهد پیکسلی (ارتفاع هدف لمسی، تب غیرفعال، CTA بزرگ‌متن، پیام rate-limit).

## ۳) بدهی دادهٔ specs + تمیزسازی رد دیجی‌کالا
- `6d0b090` ویژگی `features[]` روی `PUT/POST /api/admin/products` (tx replace، سقف ۲۰) + ادیتور «مشخصات فنی» در فرم ادمین.
- بک‌فیلد **۱۳۸/۱۳۸** داخل کانتینر prod با JWT کوتاه‌عمر — **zero-fabrication** (هر ویژگی توکن عین عنوان خودِ محصول) → ۱۲۲ محصول دارای specs.
- تمیزسازی (دستور کاربر: بدون منبع دیجی‌کالا): حذف سطر «منبع استعلام … دیجی‌کالا» از ۱۲۵ توضیح، `DK-<id>` → `JB-<id>`، تصاویر `dk-*` → `p-*` روی host **و** docroot nginx، sweep ۱۳۸ تصویر: ۰ شکسته.
- `14b60b7` اثبات: رندر جدول مشخصات + `additionalProperty` در JSON-LD روی prod؛ باز‌اسکن API: provenance 0 / DK- 0 / dk- 0.

## ۴) SEO روزانه
- robots/sitemap سالم (۱۹۲ `loc`).
- `9b54367` یافتهٔ واقعی: ۸ مسیر استاتیک sitemap عنوان ژنریک SSR می‌گرفتند → `STATIC_ROUTE_META`؛ اثبات curl: ۸/۸ عنوان متمایز + canonical/og منطبق.

## ۵) مسیر دریافت ریویو (صادقانه)
- `4872543` CTA «ثبت نظر» روی هر قلم سفارشِ `delivered` + لینک عمیق `?writeReview=1`.
- `61e93b8` فیکس: تب reviews باز می‌شد ولی فرم mount نمی‌شد (lazy-mount) → تب اولیه از پارامتر؛ پارامتر با `replaceState` پاک می‌شود (رفرش‌سیف).

## ۶) بهداشت زیرساخت Hermes
- رفع اشتراک توکن تلگرام بین `default`/`code-pro`/`novin-khodro` (کامنت + بکاپ `.bak-telegram-dedup`).
- ریشهٔ هشدار «mixed sys.modules»: receipt آپدیت نیمه‌کاره؛ `hermes gateway restart` دشارژ نمی‌کند → `hermes update` → «pending fleet restart completed»؛ `_pending_fleet_restart_needed()` = False، دروازه روی `b6b53c69a6` = HEAD.
- درس در skill ثبت شد.

## ۷) E2E واقعی روی prod + بست روز
- `6a1a930` اسکریپت `scripts/probes/e2e-prod-0913.mjs`: home → PDP(مشخصات) → افزودن به سبد → استپر ۱→۲ → خطای کوپن فارسی تک‌سطحی → checkout → submit بدون pageerror → JSON-LD = **8/8 PASS** (موبایل ۳۹۰px).

## ۸) چرخش F — میکروفلو checkout/payment
- probe روی sandbox با snapshot پرود، ۱۲ سناریو → **12/12**.
- `57d7ad3` پنج فیکس: ۴۰۱ مهمان → کپی فارسی + هدایت `/login` (نشت «Unauthorized…»)، نرمال‌سازی تلفن در blur، ریدایرکت‌های انگلیسی `payment/verify` → فارسی، گارد رندر `message` callback (فقط فارسی)، و `latin-nums` (`ss01: off`) + تعریف گمشدهٔ `.dir-ltr` تا شناسهٔ ماشینی (`ORD-…-71A7`) با گلیف فارسی رندر نشود.
- `324f9c5` ledger + راند؛ گیت + دیپلوی + بازآزمون prod.

## ۹) بازبینی کد پنل ادمین (۴ باگ اثبات‌شده)
- `b4ed0f8`: ① `DELETE /reviews/:id` امتیاز محصول را بازمحاسبه نمی‌کرد (ویترین `3.0/2` می‌ماند در حالی‌که باید `5.0/1`) ② `DELETE /products/:id` روی محصول سفارش‌شده → `500 FOREIGN KEY constraint failed` → اکنون `409 PRODUCT_IN_ORDERS` با پیام فارسی ③ `orders/bulk-delete` موجودی/امتیاز را برنمی‌گرداند (استوک ۵۰ ماند به‌جای ۵۲) → هم‌ارز مسیر cancel تکی ④ پوشش audit: ۸ رویداد جدید.
- ۳ تست رگرسیون؛ `3b03e2c` گزارش `docs/ADMIN-CODE-REVIEW-2026-09-13.md`.

## ۱۰) R1 + R2 (امنیت مالک + صفحه‌بندی)
- `d93f79a`: **R1** محافظت حساب مالک با `OWNER_USER_ID` (env) → `403 OWNER_PROTECTED` روی role/password/points + cloaking در `GET /users`؛ سیم‌کشی env/example/deploy.sh + بازسازی کانتینر. **اثبات زندهٔ prod**: ادمین غیرمالک → ۴۰۳×۳ و مالک در لیست نیست؛ مالک → می‌بیند.
- **R2** صفحه‌بندی اختیاری `?page=&limit=` (سقف ۵۰۰) + همیشه `X-Total-Count` روی ۵ لیست؛ بدون پارامتر = لیست کامل.
- `4733df5` یافتهٔ جدید **R3**: `users.joined_date` متن نمایشی فارسی است → ترتیب لیست کاربران زمانی نیست (نیاز migration `created_at`) — باز.

## ۱۱) پاک‌سازی داده‌های تستی
- **prod**: ۸ کاربر → **۳** (فقط ادمین‌ها: aidin مالک، masoume، ali)؛ ۱ سبد رهاشده؛ سفارش‌ها دست‌نخورده؛ `integrity_check=ok` و `foreign_key_check` تمیز؛ بکاپ `/home/ubuntu/backups/janebi-pre-purge-2026-09-13T16-26-05-630Z.db`.
- **لوکال (تصمیم خودم)**: ۱۹۵۱ → ۳ کاربر (۱۹۴۸ ردیف تستی شامل ۴۴۷ ادمین ساختگی، ۹ نظر تستی) + بازمحاسبهٔ امتیاز ۹ محصول؛ بکاپ `/tmp/janebi-local-pre-purge.db`.
- `c04b0ff` ابزار ماندگار `scripts/purge-test-users.cjs` (inspect/apply، بکاپ WAL-safe، تراکنش واحد) + `scripts/probes/verify-owner-protection.sh`.

## ۱۲) R3 — ترتیب زمانی لیست کاربران (بسته شد)
- ریشه: `GET /api/admin/users` روی `ORDER BY joined_date DESC` بود؛ `joined_date` متن نمایشی `Intl('fa-IR')` است (`۱۴۰۵/۶/۷`) → ترتیب **لفظی**، نه زمانی (و در DB توسعه ترکیب ISO/فارسی/NULL).
- فیکس: `users.created_at` (epoch ms) در اسکیمای هر دو دیالکت + migration `0012_users_created_at.sql` (افزودن ستون + پرکردن از epoch داخل `id` یعنی `usr-<ms>`) + ثبت `createdAt` در register و OTP + `ORDER BY COALESCE(created_at,0) DESC` (ردیف بدون تاریخ → آخر، نه «تازه‌ترین») + `scripts/backfill-user-created-at.cjs` که ردیف‌های قدیمی را با **بازحل دقیق روز** از متن جلالیِ ذخیره‌شده پر می‌کند (بدون جعل).
- تست: +۲ در `tests/api/admin-hardening.test.ts` (ترتیب بر `created_at` با تاریخ‌های نمایشی به‌عمد به‌هم‌ریخته؛ ردیف NULL آخر).
- گیت: tsc تمیز، **414 passed / 5 skipped**.
- اثبات زندهٔ prod (`scripts/probes/verify-users-chronology.sh`): ستون + journal تأیید، بک‌فیلد ۲/۳، `integrity ok`، `foreign_key_check` صفر، API مالک → ترتیب `masoume`(۲۰۲۶-۰۹-۰۵) → `ali` → `aidin` (placeholder «۱ فروردین ۱۴۰۵» → عمداً `NULL`). دقت روز تأیید شد: `1788566400000` = `۱۴۰۵/۶/۱۴` تهران.

## ۱۳) بازبینی مستقل + اصلاح residue
- نتیجهٔ بازبینی مستقل: گزارش تأیید شد (BUILD_INFO/health/DB `3|138|1|0`/parity/`STATIC_ROUTE_META`۸ مسیر/gateway بدون هشدار).
- ایراد یافته‌شده: پاک‌سازی لوکال بی‌اثر شده بود چون `npm run verify` **بعد** از آن اجرا شد و دوباره ۱۶ کاربر + ۳۳ محصول تستی ساخت (همان چیزی که `err:N` در `design-audit` می‌سازد). فیکس: `scripts/purge-test-users.cjs` حالا محصولات تستی (`test-*.jpg` / «کالای تست») را هم FK-safe و order-aware پاک می‌کند (`d4a2a7d`).
- **قاعدهٔ ثبت‌شده در skill:** purge همیشه **بعد از آخرین** verify، هرگز قبل.
- این پاس هم: بک‌فیلد R3 دوباره کاربران تستی ساخت → پاک‌سازی پایانی پس از آخرین verify اجرا شد.

## ۱۴) رفع احراز هویت git
- `git push` با خطای `Invalid username or token. Password authentication is not supported` مردود می‌شد (اعتبار کهنهٔ Keychain). `gh auth setup-git` اعتبار `gh` را در git config نشاند → push سالم.

## گیت‌های پایانی (اجرای آخر)
- `npm run verify`: ۵۶ فایل، **414 passed / 5 skipped** → ALL HARDCORE PASSED.
- `design-audit`: **8/8 PASS** (WebKit+Chromium × light/dark × 390/1280، `err:0`, `bleed 0`).
- byte-parity: `dist/server.cjs` md5 `2c3246f…` در محلی/کانتینر/دیسک وپس یکسان.
- prod: `status ok`، home/products/checkout = 200، `users=3` (هر ۳ ادمین)، BUILD_INFO همان آخرین کامیت دیپلوی‌شده.

## باز / بعدی
- خرید واقعی E2E روی prod با حساب واقعی (نیازمند تأیید کاربر؛ تا امروز فقط مهمان/سندباکس).
- (بهداشتی، غیرمسدودکننده) `drizzle/meta` با migrationهای دست‌ساز sync نیست: `db:generate` یک migration تکراری `0002_polite_proteus.sql` با DDL تکراری تولید می‌کرد؛ revert شد. تا اصلاح، migrationها دست‌ساز بمانند.

